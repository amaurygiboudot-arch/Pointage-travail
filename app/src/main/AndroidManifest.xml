<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:theme="@style/Theme.PointageTravail"
        android:label="@string/app_name"
        android:icon="@drawable/hp_icon_green"
        android:roundIcon="@drawable/hp_icon_green"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:allowBackup="true">

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.exports"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/diamond_designer_file_paths" />
        </provider>

        <activity
            android:name=".DiamondDesignerReferenceActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DiamondLabActivity"
            android:exported="false" />

    </application>
</manifest>
