package com.amaury.pointage

import android.app.Activity

/**
 * TEMPORAIRE — désactivé pour diagnostiquer le rendu des trois boutons diamant.
 * On conserve l'objet et ses appels en place afin de pouvoir réactiver la protection
 * après le test sans modifier le reste de l'application.
 */
object PrimaryButtonIsolation {
    fun install(activity: Activity) {
        // Intentionnellement vide pendant le test du rendu des facettes.
    }
}
